import React from 'react';

const Categories = () => {

  return (
    <div>
      <h3>Select a category</h3>
      <select className="category" placeholder="Select Category">
        <option>

        </option>
      </select>
    </div>
  );
};

export default Categories;